# Read-only MCP service endpoints used by policy/context graph phases.
PROVISR_MCP_POLICY_URL=http://localhost:5100
PROVISR_MCP_CLOUD_URL=http://localhost:5101

# Shared service credential; do not commit a real value.
PROVISR_MCP_SERVICE_AUTH_TOKEN=
